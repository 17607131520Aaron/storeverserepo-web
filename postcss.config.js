export default {
  plugins: {
    // Vite 内置了 autoprefixer，不需要单独配置
    // 如果需要额外的 PostCSS 插件，可以在这里添加
    // 例如：autoprefixer（需要安装：npm install -D autoprefixer）
    // 或者 postcss-split（已安装，如有需要可以配置）
    // 'postcss-split': {},
  },
};
